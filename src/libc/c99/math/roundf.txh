@c ----------------------------------------------------------------------
@node roundf, math
@findex roundf
@subheading Syntax

@example
#include <math.h>

float roundf(float x);
@end example

@subheading Description

@code{roundf} rounds @var{x} to the nearest integer value, rounding
halfway cases away from zero, regardless of the current rounding direction.

@subheading Return Value

@code{roundf} returns the rounded value of @var{x}.

@subheading Portability

@portability ansi, posix
