@c ----------------------------------------------------------------------
@node lround, math
@findex lround
@subheading Syntax

@example
#include <math.h>

long lround(double x);
@end example

@subheading Description

@code{lround} rounds @var{x} to the nearest integer value, rounding
halfway cases away from zero, regardless of the current rounding direction.

@subheading Return Value

@code{lround} returns the rounded value of @var{x}.

@subheading Portability

@portability ansi, posix
