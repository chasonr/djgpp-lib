@c ----------------------------------------------------------------------
@node fminf, math
@findex fminf
@subheading Syntax

@example
#include <math.h>

float fminf(float x, float y);
@end example

@subheading Description

@code{fminf} returns the lesser of @var{x} or @var{y}. It treats a @code{NaN}
as missing data, and returns the other argument.

@subheading Return Value

@code{fminf} returns the lesser of @var{x} or @var{y}.

@subheading Portability

@portability ansi, posix
